{
  "name": "Google-Books-Search",
  "version": "1.0.0",
  "description": "In this activity, I created a new React-based Google Books Search app. This app was created with React components, work with helper/util functions, and utilize React lifecycle methods to query and display books based on user searches. I used Node, Express and MongoDB so that users can save books to review or purchase later.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon || if-env NODE_ENV=production && npm run start:prod || npm run start:dev",
    "start:prod": "node server.js",
    "client": "cd client && npm run start",
    "install": "cd client && npm install",
    "build": "cd client && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/amcmilne/Google-Books-Search.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/amcmilne/Google-Books-Search/issues"
  },
  "homepage": "https://github.com/amcmilne/Google-Books-Search#readme",
  "dependencies": {
    "axios": "^0.21.1",
    "express": "^4.17.1",
    "mongo": "^0.1.0",
    "mongoose": "^5.12.1",
    "nodemon": "^2.0.7"
  },
  "devDependencies": {
    "favicons-webpack-plugin": "^5.0.2",
    "webpack-pwa-manifest": "^4.3.0"
  }
}
